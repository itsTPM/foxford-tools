{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "git diff --name-only -z --cached | xargs -r0 pnpm exec prettier --write && git diff --name-only -z --cached | xargs -r0 git add && pnpm type-check && pnpm lint",
            "statusMessage": "Running prettier, type-check and lint...",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
