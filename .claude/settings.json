{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "STAGED=$(git diff --name-only --cached) && echo \"$STAGED\" | xargs -r pnpm exec prettier --write && echo \"$STAGED\" | xargs -r git add && pnpm type-check && pnpm lint",
            "statusMessage": "Running prettier, type-check and lint...",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
